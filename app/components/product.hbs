<LinkTo @route="item" @model={{ @product.id }} class="product">
    <Product::Image @src={{ this.productImage }} />
    <Product::Details
        @id={{ @product.id }}
        @image={{ @product.image }}
        @name={{ @product.name }}
        @price={{ @product.price }}
    />
</LinkTo>